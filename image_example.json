{
  "builders": [
    {
      "type": "azure-arm",
      "client_id": "REPLACE-WITH-YOUR-CLIENT-ID",
      "client_secret": "REPLACE-WITH-YOUR-CLIENT-SECRET",
      "tenant_id": "REPLACE-WITH-YOUR-TENANT-ID",
      "subscription_id": "REPLACE-WITH-YOUR-SUBSCRIPTION-ID",
      "managed_image_resource_group_name": "res-group-for-images",
      "managed_image_name": "wordpress-image",
      "resource_group_name": "res-group-for-images",
      "storage_account": "diagaac94722cd21774f",
      "capture_container_name": "images",
      "os_type": "Linux",
      "image_publisher": "OpenLogic",
      "image_offer": "CentOS",
      "image_sku": "7.0",
      "ssh_pty" : "true",
      "azure_tags": {
        "dept": "Engineering",
        "task": "Image deployment"
      },
      "location": "East US",
      "vm_size": "Standard_B1ms"
    }
  ],
  "provisioners": [
    {
      "execute_command": "echo '{{user `ssh_pass`}}' | {{ .Vars }} sudo -S -E sh '{{ .Path }}'",
      "inline": [
        "yum update -y",
        "yum install -y docker nano wget git",
        "systemctl enable docker && systemctl start docker",
        "docker pull skostiuk/apache-php:5.0",
        "docker run -d -p 8080:80 skostiuk/apache-php:5.0",
        "/usr/sbin/waagent -force -deprovision+user && export HISTSIZE=0 && sync"
      ],
      "inline_shebang": "/bin/sh -x",
      "type": "shell",
      "skip_clean": true
    }
  ]
}